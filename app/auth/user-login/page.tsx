import { LoginForm } from "@/components/auth/user-login";

const Loginpage = () => {
  return (
    <div className="w-full h-full flex justify-center items-center">
      <LoginForm />
    </div>
  );
};

export default Loginpage;
